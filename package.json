{
  "name": "simple-web",
  "private": true,
  "scripts": {
    "build": "rm -rf www && mkdir www && cp -r index.html css js assets www/",
    "sync": "npm run build && npx cap sync ios",
    "test": "node tests/browser.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.52.0"
  },
  "dependencies": {
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0"
  }
}
