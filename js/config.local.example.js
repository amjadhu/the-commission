// Supabase configuration — copy this file to config.local.js and fill in your values
// from your Supabase project dashboard (Settings → API).
// config.local.js will be loaded by index.html and is ignored by git.

window.CONFIG = {
  SUPABASE_URL: 'YOUR_SUPABASE_URL',
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY'
};
